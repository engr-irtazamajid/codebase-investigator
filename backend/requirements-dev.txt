# Dev / CI — NOT installed in production Docker image
ruff==0.8.6
mypy==1.13.0
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-mock==3.14.0
httpx==0.27.2      # already in requirements.txt but pin here too for test client
